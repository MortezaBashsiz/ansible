---

- name: packages role
  include_role:
    name: packages
  vars:
    PKGS_LIST: "{{ NGINX_PKGS_LIST }}"

- name: configuring instances
  include_tasks: instances.yml
  loop: "{{ NGINX_INSTANCES }}"
  loop_control:
    loop_var: instance

